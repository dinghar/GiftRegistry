import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    contentContainer: {
        display: "flex",
        width: "100%",
        height: "100%",
        maxWidth: 500,
        paddingVertical: 30,
    }
})