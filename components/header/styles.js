import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    header: {
        backgroundColor: "white",
        padding: 30,
        width: "100%",
        height: 100,
        display: "flex",
        alignItems: "center"
    },

    headerText: {
        fontSize: 24,
    }
})