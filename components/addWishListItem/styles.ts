import { StyleSheet } from "react-native";
import { vars } from "../../styles/vars";

export const styles = StyleSheet.create({
  buttonContainer: {
    alignItems: "flex-end",
    marginRight: vars.spaceLg,
  },
});
